import type { CloseShiftInput, NozzleCustodyInput, OpenShiftInput } from '@fuelledger/shared';
import { Prisma } from '@prisma/client';
import { AppError } from '../../lib/errors.js';
import { prisma } from '../../lib/prisma.js';

const include = { station: true, manager: { select: { id: true, name: true, role: true } }, users: { include: { user: { select: { id: true, name: true, role: true } } } }, nozzleAssignments:{include:{user:{select:{id:true,name:true,role:true}},nozzle:{include:{product:true,dispenser:true}}}}, tankReadings: { include: { tank: { include: { product: true } } } }, nozzleReadings: { include: { nozzle: { include: { product: true, dispenser: true } } } } };
const exact = (ids: string[], values: Array<{ id: string }>) => ids.length === values.length && ids.every(id => values.some(value => value.id === id));
export async function bootstrap(organizationId: string,stationIds?:string[]) { const [stations, users, shifts] = await Promise.all([
  prisma.station.findMany({ where: { organizationId, active: true,...(stationIds?{id:{in:stationIds}}:{}) }, include: { configurations: { where: { active: true }, take: 1, include: { tanks: { where: { status: 'ACTIVE' }, include: { product: true } }, dispensers: { where: { status: 'ACTIVE' }, include: { nozzles: { where: { status: 'ACTIVE' }, include: { product: true } } } } } } } }),
  prisma.user.findMany({ where: { organizationId, active: true,...(stationIds?{OR:[{role:{in:['OWNER','ACCOUNTANT']}},{stationAccess:{some:{stationId:{in:stationIds}}}}]}:{}) }, select: { id: true, name: true, role: true } }),
  prisma.shift.findMany({ where: { station: { organizationId,...(stationIds?{id:{in:stationIds}}:{}) } }, orderBy: { openedAt: 'desc' }, take: 8, include }),
]); return { stations, users, shifts: shifts.map(summary) }; }
export async function openShift(organizationId: string, input: OpenShiftInput) { const station = await prisma.station.findFirst({ where: { id: input.stationId, organizationId, active: true }, include: { configurations: { where: { active: true }, take: 1, include: { tanks: { where: { status: 'ACTIVE' } }, dispensers: { where: { status: 'ACTIVE' }, include: { nozzles: { where: { status: 'ACTIVE' } } } } } } } }); if (!station) throw new AppError(404, 'STATION_NOT_FOUND', 'Choose an active station.'); const config = station.configurations[0]; if (!config) throw new AppError(400, 'STATION_NOT_CONFIGURED', 'This station needs an active configuration first.'); const tanks = config.tanks; const nozzles = config.dispensers.flatMap(dispenser => dispenser.nozzles); if (!exact(input.tankReadings.map(reading => reading.id), tanks) || !exact(input.nozzleReadings.map(reading => reading.id), nozzles)) throw new AppError(400, 'READINGS_INCOMPLETE', 'Enter one opening reading for every active tank and nozzle.'); if(!exact(input.nozzleAssignments.map(row=>row.nozzleId),nozzles)||new Set(input.nozzleAssignments.map(row=>row.nozzleId)).size!==nozzles.length)throw new AppError(400,'NOZZLE_ASSIGNMENTS_INCOMPLETE','Assign one attendant to every active nozzle.'); const users = await prisma.user.findMany({ where: { organizationId, id: { in: [input.managerId, ...input.userIds] }, active: true } }); if (users.length !== new Set([input.managerId, ...input.userIds]).size||input.nozzleAssignments.some(row=>!input.userIds.includes(row.userId))) throw new AppError(400, 'SHIFT_USER_INVALID', 'Each nozzle attendant must be on the shift team.'); if (await prisma.shift.findFirst({ where: { stationId: station.id, status: 'OPEN' } })) throw new AppError(409, 'SHIFT_ALREADY_OPEN', 'Close the current open shift before opening another one.'); const latest = await prisma.shift.aggregate({ where: { stationId: station.id }, _max: { shiftNumber: true } }); const shift = await prisma.shift.create({ data: { stationId: station.id, configurationId: config.id, shiftNumber: (latest._max.shiftNumber ?? 0) + 1, managerId: input.managerId, status: 'OPEN', openingCash: new Prisma.Decimal(input.openingCash), notes: input.notes || null, users: { create: Array.from(new Set(input.userIds)).map(userId => ({ userId })) }, nozzleAssignments:{create:input.nozzleAssignments.map(row=>({nozzleId:row.nozzleId,userId:row.userId}))}, tankReadings: { create: input.tankReadings.map(reading => ({ tankId: reading.id, openingDip: new Prisma.Decimal(reading.value) })) }, nozzleReadings: { create: input.nozzleReadings.map(reading => ({ nozzleId: reading.id, openingMeter: new Prisma.Decimal(reading.value) })) } }, include }); return summary(shift); }
export async function closeShift(organizationId: string, id: string, input: CloseShiftInput) { const shift = await prisma.shift.findFirst({ where: { id, station: { organizationId } }, include }); if (!shift) throw new AppError(404, 'SHIFT_NOT_FOUND', 'This shift was not found.'); if (shift.status !== 'OPEN') throw new AppError(409, 'SHIFT_NOT_OPEN', 'Only an open shift can be closed.'); if (!exact(input.tankReadings.map(reading => reading.id), shift.tankReadings.map(reading => ({ id: reading.tankId }))) || !exact(input.nozzleReadings.map(reading => reading.id), shift.nozzleReadings.map(reading => ({ id: reading.nozzleId })))) throw new AppError(400, 'READINGS_INCOMPLETE', 'Enter one closing reading for every active tank and nozzle.'); for (const reading of input.tankReadings) { const opening = shift.tankReadings.find(value => value.tankId === reading.id)!; if (reading.value < Number(opening.openingDip)) throw new AppError(400, 'READING_INVALID', 'A closing tank reading cannot be below its opening reading.'); } for (const reading of input.nozzleReadings) { const opening = shift.nozzleReadings.find(value => value.nozzleId === reading.id)!; if (reading.value < Number(opening.openingMeter)) throw new AppError(400, 'READING_INVALID', 'A closing meter reading cannot be below its opening reading.'); } const meteredSales=await prisma.sale.groupBy({by:['nozzleId'],where:{shiftId:id,kind:'METERED'},_sum:{quantity:true}});for(const reading of input.nozzleReadings){const opening=shift.nozzleReadings.find(value=>value.nozzleId===reading.id)!;const meterMovement=reading.value-Number(opening.openingMeter);const recorded=Number(meteredSales.find(sale=>sale.nozzleId===reading.id)?._sum.quantity??0);if(Math.abs(meterMovement-recorded)>.001)throw new AppError(409,'METER_SALES_MISMATCH',`Nozzle ${opening.nozzle.code} moved ${meterMovement.toLocaleString()} L, but ${recorded.toLocaleString()} L of metered sales were recorded. Record or correct the sales before closing the shift.`);} const closed = await prisma.$transaction(async tx => { await Promise.all(input.tankReadings.map(reading => tx.shiftTankReading.update({ where: { shiftId_tankId: { shiftId: id, tankId: reading.id } }, data: { closingDip: new Prisma.Decimal(reading.value) } }))); await Promise.all(input.nozzleReadings.map(reading => tx.shiftNozzleReading.update({ where: { shiftId_nozzleId: { shiftId: id, nozzleId: reading.id } }, data: { closingMeter: new Prisma.Decimal(reading.value) } }))); return tx.shift.update({ where: { id }, data: { status: 'RECONCILIATION_REQUIRED', closingCash: new Prisma.Decimal(input.closingCash), closedAt: new Date(), notes: input.notes || shift.notes }, include }); }); return summary(closed); }
function summary(shift: any) { const volume = shift.nozzleReadings.reduce((sum: number, reading: any) => sum + (reading.closingMeter === null ? 0 : Number(reading.closingMeter) - Number(reading.openingMeter)), 0); return { ...shift, summary: { fuelVolume: volume, tanksCaptured: shift.tankReadings.filter((reading: any) => reading.closingDip !== null).length, nozzlesCaptured: shift.nozzleReadings.filter((reading: any) => reading.closingMeter !== null).length } }; }
export async function updateNozzleCustody(organizationId:string,id:string,input:NozzleCustodyInput){const shift=await prisma.shift.findFirst({where:{id,station:{organizationId}},include:{users:true,nozzleReadings:true}});if(!shift)throw new AppError(404,'SHIFT_NOT_FOUND','This shift was not found.');if(shift.status!=='OPEN')throw new AppError(409,'SHIFT_NOT_OPEN','Nozzle assignments can only change during an open shift.');if(!exact(input.assignments.map(row=>row.nozzleId),shift.nozzleReadings.map(row=>({id:row.nozzleId})))||new Set(input.assignments.map(row=>row.nozzleId)).size!==shift.nozzleReadings.length)throw new AppError(400,'NOZZLE_ASSIGNMENTS_INCOMPLETE','Assign every active nozzle exactly once.');const team=new Set([shift.managerId,...shift.users.map(row=>row.userId)]);if(input.assignments.some(row=>!team.has(row.userId)))throw new AppError(400,'ATTENDANT_NOT_ON_SHIFT','Every nozzle attendant must be on this shift.');await prisma.$transaction(async tx=>{await tx.shiftNozzleAssignment.deleteMany({where:{shiftId:id}});await tx.shiftNozzleAssignment.createMany({data:input.assignments.map(row=>({shiftId:id,nozzleId:row.nozzleId,userId:row.userId}))});});const updated=await prisma.shift.findUnique({where:{id},include});return summary(updated);}
