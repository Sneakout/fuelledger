import { Router } from 'express';import { z } from 'zod';import { AppError } from '../lib/errors.js';import { assertStationAccess,permittedStationIds } from '../lib/station-access.js';import { authenticate } from '../middleware/authenticate.js';import { bootstrap } from '../modules/dashboard/service.js';const query=z.object({stationId:z.string().cuid().optional()});export const dashboardRouter=Router();dashboardRouter.use(authenticate);dashboardRouter.get('/bootstrap',async(req,res)=>{const parsed=query.safeParse(req.query);if(!parsed.success)throw new AppError(400,'STATION_INVALID','Choose a valid fuel station.');if(parsed.data.stationId)assertStationAccess(req.user!,parsed.data.stationId);res.json(await bootstrap(req.user!.organization.id,permittedStationIds(req.user!),parsed.data.stationId));});
