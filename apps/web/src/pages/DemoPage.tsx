import { useState,type FormEvent } from 'react';import { ArrowLeft,Clock3,Eye,Fuel,Mail,ShieldCheck } from 'lucide-react';import { Link,Navigate } from 'react-router-dom';import { useAuth } from '../components/AuthProvider';
export function DemoPage(){const{user,startDemo}=useAuth();const[contact,setContact]=useState('');const[busy,setBusy]=useState(false);const[error,setError]=useState('');if(user)return <Navigate to="/" replace/>;async function submit(event:FormEvent){event.preventDefault();setBusy(true);setError('');try{await startDemo({contact});}catch(item){setError(item instanceof Error?item.message:'Unable to start the demo.');}finally{setBusy(false);}}return <main className="demo-entry"><section><div className="login-brand"><span className="brand-mark"><Fuel size={24}/></span>FuelLedger</div><span className="eyebrow light">Interactive product tour</span><h1>See a fuel station<br/><em>running clearly.</em></h1><p>Explore a populated station—from daily sales and wet stock to reconciliation, accounting, and owner reports.</p><div className="demo-promises"><span><Clock3/><b>48-hour access</b><small>Return on this device anytime</small></span><span><Eye/><b>Every workflow</b><small>Realistic demonstration data</small></span><span><ShieldCheck/><b>Safe & read-only</b><small>Nothing in the demo can be changed</small></span></div></section><section><form onSubmit={submit}><Link to="/login" className="back-link"><ArrowLeft/> Back to sign in</Link><span className="eyebrow">No account required</span><h2>Start your private demo</h2><p>Enter a work email or mobile number. We use it to create and manage your 48-hour demo access.</p>{error&&<div className="form-error" role="alert">{error}</div>}<label>Email or mobile number<div className="input-wrap"><Mail/><input value={contact} onChange={event=>setContact(event.target.value)} placeholder="you@company.com or +91…" autoComplete="email" required/></div></label><button className="primary" disabled={busy}>{busy?'Preparing your demo…':'Explore FuelLedger'}</button><small className="demo-privacy">By continuing, you agree that FuelLedger may use this contact to support your demo. No password is required.</small></form></section></main>;}
